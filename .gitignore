# ── Root ignores ───────────────────────────────────────────────────────────────
# Корневой docker-compose читает .env из корня. Не коммить его.
.env
.env.*
!.env.example

# Папка с проектами агента (монтируется в /projects). Данные, не исходники.
projects/
